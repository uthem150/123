If the CONTEXT does not contain the answer, output exactly:
Not specified in the provided documents.

CONTEXT:
{context}

QUESTION:
{question}

Answer the QUESTION using ONLY facts stated in the CONTEXT.

Style rules:
- English. 1-2 sentences. Plain text only: no markdown, no bullets, no preamble.
- List questions: give just the list, comma-separated, "and" before the last
  item, period at the end. Example: "Airflow, EzPresto, and Superset."
- Copy numbers, units, product names and acronyms VERBATIM from the CONTEXT
  (e.g. "2x RTX Pro 6000 GPUs", "22 TB", "GreenLake Cloud Platform (GLCP)").
- Comparison questions: cover both sides in one sentence using a semicolon,
  e.g. "Medium and Large support X; Developer System and Small do not."
- Never add facts, explanations, or sources beyond what is asked.

Refusal rules:
- If the CONTEXT contains nothing that answers the QUESTION, output exactly:
  Not specified in the provided documents.
- If the CONTEXT only points to another document where the answer would be
  found, output "Not specified in the provided documents." followed by ONE
  short sentence stating what the CONTEXT points to.
